"""Offline community → topic → evidence decision report."""

from __future__ import annotations

import json
from pathlib import Path
from typing import Any, Mapping


def render_html(analysis: Mapping[str, Any], output_path: str | Path) -> Path:
    """Render an offline graph, topic preview, and evidence-grounded full report."""
    output = Path(output_path)
    output.parent.mkdir(parents=True, exist_ok=True)
    document = dict(analysis)
    payload = json.dumps(document, ensure_ascii=False, separators=(",", ":"))
    payload = payload.replace("&", "\\u0026").replace("<", "\\u003c").replace(">", "\\u003e")
    html = r'''<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Opportunity Radar｜社区话题图谱</title>
<style>
:root{--bg:#f3f0ea;--paper:#fbfaf7;--ink:#25221f;--muted:#837c72;--line:#ddd7ce;--orange:#e68c3e;--orange2:#b76627;--blue:#516a7b;--green:#477762;--shadow:0 18px 60px rgba(44,36,28,.12)}
*{box-sizing:border-box}html,body{height:100%;margin:0}body{overflow:hidden;background:var(--bg);color:var(--ink);font:14px/1.55 Inter,"Microsoft YaHei",system-ui,sans-serif}button,input{font:inherit}button{color:inherit}
header{height:58px;padding:0 24px;border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;background:rgba(243,240,234,.96);position:relative;z-index:5}.logo{font:800 18px/1 ui-monospace,monospace}.logo b{color:var(--orange)}.brand{font:11px ui-monospace,monospace;color:var(--muted);letter-spacing:.08em}
.page{height:calc(100% - 58px);display:grid;grid-template-columns:300px minmax(0,1fr) 0;transition:grid-template-columns .28s ease}.page.detail-open{grid-template-columns:300px minmax(360px,1fr) 420px}.page.report-open{grid-template-columns:280px minmax(320px,.62fr) minmax(650px,1.38fr)}
.left{padding:25px 20px;border-right:1px solid var(--line);overflow:auto;background:#f7f4ef}.left-kicker,.eyebrow{font:700 10px ui-monospace,monospace;letter-spacing:.12em;text-transform:uppercase;color:var(--orange2)}.left h2{font-size:25px;margin:8px 0}.intro{font-size:12px;color:var(--muted);margin:0 0 18px}.search{width:100%;padding:10px 12px;border:1px solid var(--line);border-radius:9px;background:#fff;outline:none}.search:focus{border-color:var(--orange)}.reset{border:0;background:none;color:var(--orange2);padding:12px 0;cursor:pointer}.section-title{font-size:10px;font-weight:800;color:var(--muted);letter-spacing:.08em;text-transform:uppercase;margin:16px 0 8px}.community-row,.topic-row{width:100%;border:0;background:transparent;border-radius:8px;padding:9px 8px;display:flex;gap:9px;text-align:left;cursor:pointer}.community-row:hover,.topic-row:hover,.topic-row.active{background:#ece6dd}.dot,.topic-dot{width:11px;height:11px;border:2px solid #756d64;border-radius:50%;margin-top:4px;flex:none}.topic-dot{border:0;background:var(--orange)}.row-body{min-width:0;display:flex;flex-direction:column}.row-name{font-weight:700}.row-meta{font-size:11px;color:var(--muted)}.empty{color:var(--muted);font-size:12px;padding:12px 0}.legend{font-size:11px;color:var(--muted);border-top:1px solid var(--line);margin-top:20px;padding-top:14px}.legend-line{display:flex;gap:8px;margin:6px 0}
.center{position:relative;min-width:0;overflow:hidden;background:radial-gradient(circle at 50% 45%,#faf8f4 0,#f3f0ea 62%)}.toolbar{position:absolute;left:18px;top:16px;z-index:2}.metric{display:block;max-width:900px;border:1px solid var(--line);background:rgba(255,255,255,.78);padding:7px 10px;border-radius:8px;font:11px ui-monospace,monospace;color:#645d55}.graph{width:100%;height:100%}.graph-help{position:absolute;bottom:20px;left:50%;transform:translateX(-50%);font-size:11px;color:var(--muted);background:rgba(255,255,255,.85);padding:7px 12px;border-radius:999px}.edge{stroke:#d9cdbc;stroke-width:1}.community-node,.topic-node{cursor:pointer}.community-node circle{fill:#f8f6f1;stroke:#796f65;stroke-width:1.5}.community-node.active circle{fill:#fff7ed;stroke:var(--orange);stroke-width:2}.node-label,.node-count,.topic-label{text-anchor:middle;pointer-events:none}.node-label{font-size:13px;font-weight:800}.node-count{font-size:10px;fill:#8c847a}.topic-label{font-size:11px;fill:#554e47}.topic-node circle{stroke:#fff;stroke-width:2}.topic-node.active circle{stroke:#7a3c12;stroke-width:3}
.right{position:relative;min-width:0;border-left:1px solid var(--line);background:var(--paper);overflow:auto;box-shadow:var(--shadow);transition:opacity .2s}.right.closed{opacity:0;pointer-events:none}.close{position:sticky;float:right;right:16px;top:14px;z-index:8;width:32px;height:32px;border:1px solid var(--line);border-radius:50%;background:#fff;cursor:pointer}.detail{padding:30px 28px 54px}.detail h2{font-size:28px;line-height:1.15;margin:8px 40px 6px 0}.en{font-size:12px;color:var(--muted)}.summary{font-size:15px;line-height:1.75;margin:18px 0}.tags{display:flex;gap:7px;flex-wrap:wrap;margin:15px 0}.tag{font-size:10px;background:#ede8df;border-radius:999px;padding:4px 8px}.tag.formal{background:#e2efe8;color:#2b6b4f}.tag.weak{background:#f5e8d7;color:#87521f}.field{border-top:1px solid var(--line);padding:15px 0}.field h3,.report-section h3{font-size:13px;margin:0 0 9px}.field ul,.report-section ul{margin:0;padding-left:18px}.field li,.report-section li{margin:5px 0}.preview-stats{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin:15px 0}.stat{border:1px solid var(--line);background:#fff;border-radius:9px;padding:10px}.stat .k{font-size:10px;color:var(--muted)}.stat .v{font-size:18px;font-weight:800}.preview-action{position:sticky;bottom:-54px;margin:22px -28px -54px;padding:30px 28px 14px;background:linear-gradient(transparent,#fbfaf7 24%)}.primary{width:100%;border:0;border-radius:9px;background:var(--ink);color:#fff;padding:12px;cursor:pointer;font-weight:700}.primary:hover{background:var(--orange2)}.topic-mini{display:flex;gap:9px;padding:9px 0;border-bottom:1px solid #ebe6df;cursor:pointer}.topic-mini span:nth-child(2){flex:1}
.page.report-open .detail{padding:28px 38px 70px}.report-topbar{display:flex;justify-content:space-between;align-items:center;padding-bottom:15px;border-bottom:1px solid var(--line)}.report-back{border:0;background:none;color:var(--orange2);cursor:pointer;padding:0}.report-kicker{font:10px ui-monospace,monospace;color:var(--muted)}.report-hero{padding:28px 0 20px;display:grid;grid-template-columns:1fr 180px;gap:25px}.report-hero h2{font-size:34px;margin:6px 0}.report-score{border-left:3px solid var(--orange);background:#fff7ed;padding:15px;border-radius:0 9px 9px 0}.report-score .n{font-size:31px;font-weight:800}.report-score small{font-size:12px}.report-section{padding:23px 0;border-top:1px solid var(--line)}.report-section h3{font-size:17px}.report-section h3 small{font-size:11px;color:var(--muted);font-weight:500}.report-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:9px}.report-card{border:1px solid var(--line);background:#fff;border-radius:9px;padding:12px}.report-card .k{font-size:10px;color:var(--muted)}.report-card .v{font-weight:750;margin-top:4px}.report-two{display:grid;grid-template-columns:1fr 1fr;gap:10px}.claim-row{border:1px solid var(--line);background:#fff;border-radius:9px;padding:12px;margin:8px 0}.claim-text{font-weight:650}.claim-source{font-size:11px;margin-top:6px}.claim-source a,.evidence a{color:var(--orange2)}.note{font-size:11px;color:#745b3f;background:#f8eedf;border-radius:7px;padding:9px 11px;margin-top:9px}.evidence-list{display:grid;grid-template-columns:1fr 1fr;gap:10px}.evidence{border:1px solid var(--line);background:#fff;border-radius:9px;padding:12px}.evidence .en-text{font-size:13px;margin-top:8px}.evidence .zh{font-size:12px;color:var(--muted);margin-top:5px}.status-fact{color:var(--green)}.status-inference{color:#936223}.status-unknown{color:var(--muted)}.footer{font-size:11px;color:var(--muted);border-top:1px solid var(--line);padding-top:16px;margin-top:25px}
@media(max-width:1000px){.page.detail-open,.page.report-open{grid-template-columns:250px 0 minmax(0,1fr)}.center{min-width:0}.report-grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:700px){header .brand{display:none}.page,.page.detail-open,.page.report-open{grid-template-columns:0 minmax(0,1fr) 0}.page.detail-open,.page.report-open{grid-template-columns:0 0 minmax(0,1fr)}.left{display:none}.detail,.page.report-open .detail{padding:25px 20px 60px}.report-hero,.report-two,.evidence-list{grid-template-columns:1fr}}
</style></head><body>
<header><div class="logo">Opportunity Radar<b>.</b></div><div class="brand">SUNCENTAUTO · DIESEL PICKUP VOC</div></header>
<div class="page" id="page">
  <aside class="left"><div class="left-kicker" id="scopeLabel">Community radar</div><h2>社区—话题图谱</h2><p class="intro">先选择社区，再查看由真实帖子和评论归并出的用户任务与问题。所有判断均可下钻到 Reddit 证据。</p><input id="search" class="search" placeholder="搜索社区、话题、车型或关键词…" autocomplete="off"><button id="reset" class="reset" type="button">← 回到全局社区图</button><div class="section-title">已选社区</div><div id="communityList"></div><div class="section-title" id="topicTitle">选择社区后显示话题</div><div id="topicList"><div class="empty">当前未选择社区</div></div><div class="legend"><div class="legend-line"><i class="dot"></i><span>空心圆：社区</span></div><div class="legend-line"><i class="topic-dot"></i><span>实心圆：社区内话题</span></div><div class="legend-line">未选择的社区不会展开话题</div></div></aside>
  <main class="center"><div class="toolbar"><span class="metric" id="metrics"></span></div><svg id="graph" class="graph" viewBox="0 0 1000 720" role="img" aria-label="社区与话题关系图"></svg><div class="graph-help" id="graphHelp">点击一个社区，展开它的话题</div></main>
  <aside id="right" class="right closed"><button class="close" id="close" type="button" aria-label="关闭">×</button><div id="detail" class="detail"></div></aside>
</div>
<script id="analysis-data" type="application/json">__PAYLOAD__</script>
<script>
const DATA=JSON.parse(document.getElementById('analysis-data').textContent||'{}');
const TOPICS=Array.isArray(DATA.topics)?DATA.topics.filter(Boolean):[];
const norm=v=>String(v??'').trim().toLowerCase().replace(/^r\//,'');
const arr=v=>Array.isArray(v)?v.filter(Boolean):[];
const obj=v=>v&&typeof v==='object'&&!Array.isArray(v)?v:{};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const communityNames=new Map();
for(const raw of arr(DATA.communities).concat(TOPICS.map(t=>t.community||'')).filter(Boolean).map(String)){const key=norm(raw);if(!communityNames.has(key))communityNames.set(key,raw.replace(/^r\//i,''))}
const COMMUNITIES=Array.from(communityNames.values());
const byCommunity=new Map(COMMUNITIES.map(c=>[norm(c),TOPICS.filter(t=>norm(t.community)===norm(c))]));
const page=document.getElementById('page'),svg=document.getElementById('graph'),search=document.getElementById('search'),reset=document.getElementById('reset'),closeBtn=document.getElementById('close'),right=document.getElementById('right'),detail=document.getElementById('detail'),communityList=document.getElementById('communityList'),topicList=document.getElementById('topicList'),topicTitle=document.getElementById('topicTitle'),metrics=document.getElementById('metrics'),graphHelp=document.getElementById('graphHelp'),scopeLabel=document.getElementById('scopeLabel');
const NS='http://www.w3.org/2000/svg';let selectedCommunity='',selectedTopic='',currentView='graph',applyingHash=false;
const communityTopics=c=>byCommunity.get(norm(c))||[];
const topicMatches=(t,q)=>JSON.stringify(t).toLowerCase().includes(q);
const findTopic=id=>TOPICS.find(t=>String(t.topic_id)===String(id));
function node(tag,attrs={}){const n=document.createElementNS(NS,tag);for(const [k,v] of Object.entries(attrs))n.setAttribute(k,v);return n}
function setHash(view){if(applyingHash)return;const p=new URLSearchParams();if(selectedCommunity)p.set('community',selectedCommunity);if(selectedTopic)p.set('topic',selectedTopic);if(view&&view!=='graph')p.set('view',view);const next=p.toString();if(location.hash.slice(1)!==next)location.hash=next}
function openPanel(){right.classList.remove('closed');page.classList.add('detail-open')}
function closePanel(sync=true){selectedCommunity='';selectedTopic='';currentView='graph';right.classList.add('closed');page.classList.remove('detail-open','report-open');renderSide();renderGraph();if(sync)setHash('graph')}
function selectCommunity(name,sync=true){selectedCommunity=String(name).replace(/^r\//i,'');selectedTopic='';currentView='community';page.classList.remove('report-open');openPanel();renderSide();renderGraph();showCommunityDetail(selectedCommunity);if(sync)setHash('community')}
function selectTopic(id,sync=true){const t=findTopic(id);if(!t)return;selectedCommunity=String(t.community).replace(/^r\//i,'');selectedTopic=String(t.topic_id);currentView='preview';page.classList.remove('report-open');openPanel();renderSide();renderGraph();showTopicPreview(t);if(sync)setHash('preview')}
function openTopicReport(id=selectedTopic,sync=true){const t=findTopic(id);if(!t)return;selectedCommunity=String(t.community).replace(/^r\//i,'');selectedTopic=String(t.topic_id);currentView='report';openPanel();page.classList.add('report-open');renderSide();renderGraph();showTopicDetail(t);if(sync)setHash('report')}
function returnToPreview(sync=true){const t=findTopic(selectedTopic);if(!t){closePanel(sync);return}currentView='preview';page.classList.remove('report-open');showTopicPreview(t);renderSide();renderGraph();if(sync)setHash('preview')}
function applyHash(){applyingHash=true;try{const p=new URLSearchParams(location.hash.slice(1)),community=p.get('community'),topic=p.get('topic'),view=p.get('view');if(topic&&findTopic(topic)){view==='report'?openTopicReport(topic,false):selectTopic(topic,false)}else if(community&&COMMUNITIES.some(c=>norm(c)===norm(community))){selectCommunity(community,false)}else closePanel(false)}finally{applyingHash=false}}
function renderSide(){const q=search.value.trim().toLowerCase();communityList.innerHTML=COMMUNITIES.filter(c=>!q||norm(c).includes(q)||communityTopics(c).some(t=>topicMatches(t,q))).map(c=>`<button class="community-row" data-community="${esc(c)}"><i class="dot"></i><span class="row-body"><span class="row-name">r/${esc(c)}</span><span class="row-meta">${communityTopics(c).length} 个话题</span></span></button>`).join('')||'<div class="empty">没有匹配社区</div>';communityList.querySelectorAll('[data-community]').forEach(b=>b.addEventListener('click',()=>selectCommunity(b.dataset.community)));if(!selectedCommunity){topicTitle.textContent='选择社区后显示话题';topicList.innerHTML='<div class="empty">当前未选择社区</div>';return}const ts=communityTopics(selectedCommunity).filter(t=>!q||topicMatches(t,q));topicTitle.textContent=`r/${selectedCommunity} · ${communityTopics(selectedCommunity).length} 个话题`;topicList.innerHTML=ts.length?ts.slice().sort((a,b)=>(Number(b.heat_score)||0)-(Number(a.heat_score)||0)).map(t=>`<button class="topic-row ${String(t.topic_id)===selectedTopic?'active':''}" data-topic="${esc(t.topic_id)}"><i class="topic-dot"></i><span class="row-body"><span class="row-name">${esc(t.label_zh||t.label_en||'未命名话题')}</span><span class="row-meta">${esc(t.trend||'unknown')} · 热度 ${esc(t.heat_score||0)} · ${esc(t.post_count||0)} 帖</span></span></button>`).join(''):'<div class="empty">该社区暂无达到证据门槛的话题</div>';topicList.querySelectorAll('[data-topic]').forEach(b=>b.addEventListener('click',()=>selectTopic(b.dataset.topic)))}
function communityPositions(){const n=COMMUNITIES.length||1,out=[];for(let i=0;i<n;i++){const angle=-Math.PI/2+Math.PI*2*i/n,r=n<=2?170:240;out.push({x:500+Math.cos(angle)*r,y:355+Math.sin(angle)*r})}return out}
function topicPositions(ts){const out=[];let index=0,ring=0;while(index<ts.length){const count=Math.min(7,ts.length-index),radius=145+ring*90;for(let j=0;j<count;j++){const angle=-Math.PI/2+Math.PI*2*j/count+(ring%2?Math.PI/7:0);out.push({x:500+Math.cos(angle)*radius,y:360+Math.sin(angle)*radius});index++}ring++}return out}
function renderGraph(){svg.innerHTML='';const edges=node('g'),nodes=node('g');svg.append(edges,nodes);if(!selectedCommunity){graphHelp.textContent='点击一个社区，展开它的话题';const ps=communityPositions();COMMUNITIES.forEach((c,i)=>{const ts=communityTopics(c),p=ps[i],r=36+Math.min(32,ts.length*4),g=node('g',{class:'community-node'});g.append(node('circle',{cx:p.x,cy:p.y,r}));const label=node('text',{x:p.x,y:p.y+4,class:'node-label'});label.textContent='r/'+c;g.append(label);const count=node('text',{x:p.x,y:p.y+r+18,class:'node-count'});count.textContent=ts.length+' 个话题';g.append(count);g.addEventListener('click',()=>selectCommunity(c));nodes.append(g)});return}graphHelp.textContent='点击话题先在右侧预览；在预览底部进入完整报告';const ts=communityTopics(selectedCommunity),center={x:500,y:360},cr=42+Math.min(30,ts.length*3),cg=node('g',{class:'community-node active'});cg.append(node('circle',{cx:center.x,cy:center.y,r:cr}));const cl=node('text',{x:center.x,y:center.y+4,class:'node-label'});cl.textContent='r/'+selectedCommunity;cg.append(cl);const cc=node('text',{x:center.x,y:center.y+cr+18,class:'node-count'});cc.textContent=ts.length+' 个话题';cg.append(cc);nodes.append(cg);topicPositions(ts).forEach((p,i)=>{const t=ts[i],r=12+Math.min(18,Math.sqrt(Number(t.heat_score)||0)*1.1);edges.append(node('line',{x1:center.x,y1:center.y,x2:p.x,y2:p.y,class:'edge'}));const g=node('g',{class:'topic-node '+(String(t.topic_id)===selectedTopic?'active':'')});g.append(node('circle',{cx:p.x,cy:p.y,r,fill:t.status==='formal'?'#e68c3e':'#cda06b'}));const label=node('text',{x:p.x,y:p.y+r+16,class:'topic-label'}),raw=String(t.label_zh||t.label_en||'未命名话题');label.textContent=raw.length>18?raw.slice(0,17)+'…':raw;g.append(label);g.addEventListener('click',e=>{e.stopPropagation();selectTopic(t.topic_id)});nodes.append(g)})}
function list(values,empty='当前证据未确认'){const v=arr(values);return `<ul>${v.length?v.map(x=>`<li>${esc(typeof x==='object'?x.text||JSON.stringify(x):x)}</li>`).join(''):`<li class="status-unknown">${esc(empty)}</li>`}</ul>`}
function claimSection(title,key,t,fallback=[],note=''){const claims=arr(obj(t.claim_evidence)[key]);return `<section class="report-section"><h3>${esc(title)}</h3>${claims.length?claims.map(c=>{const ce=obj(c),sources=arr(ce.evidence);return `<div class="claim-row"><div class="claim-text">${esc(ce.text||'未知')}</div>${sources.length?`<div class="claim-source">${sources.slice(0,3).map(e=>`<a href="${esc(obj(e).url||'#')}" target="_blank" rel="noreferrer">查看证据 ↗</a>`).join(' · ')}</div>`:''}</div>`}).join(''):list(fallback)}${note?`<div class="note">${esc(note)}</div>`:''}</section>`}
function topicStats(t){return {posts:Number(t.post_count||0),authors:Number(t.author_count||0),commenters:Number(t.commenter_count||0),participants:t.participant_count??'未单独计算',comments:t.collected_comment_count??'未单独计算',evidence:arr(t.evidence).length}}
function decisionOf(t){const d=obj(t.product_decision);return {type:d.type||'暂不形成产品机会',status:d.status||'unknown',rationale:d.rationale||'当前证据没有给出可验证的产品判断。'}}
function showCommunityDetail(name){const ts=communityTopics(name).slice().sort((a,b)=>(Number(b.heat_score)||0)-(Number(a.heat_score)||0)),cm=obj(obj(DATA.report_metrics).communities)[name]||{};detail.innerHTML=`<div class="eyebrow">Reddit 社区 · 人群入口</div><h2>r/${esc(name)}</h2><div class="en">${esc(ts.length)} 个话题 · ${esc(cm.scanned_post_count??'未知')} 篇扫描帖 · ${esc(cm.deep_read_post_count??'未知')} 篇深读帖</div><div class="summary">该页只显示当前选择社区的话题。点击下方话题先查看决策摘要，再决定是否进入完整证据报告。</div><section class="field"><h3>社区内话题</h3>${ts.length?ts.map(t=>`<div class="topic-mini" data-topic="${esc(t.topic_id)}"><i class="topic-dot"></i><span>${esc(t.label_zh||t.label_en||'未命名话题')}</span><span class="row-meta">${esc(t.post_count||0)} 帖</span></div>`).join(''):'<div class="empty">当前没有达到证据门槛的话题</div>'}</section>`;detail.querySelectorAll('[data-topic]').forEach(b=>b.addEventListener('click',()=>selectTopic(b.dataset.topic)))}
function showTopicPreview(t){const s=topicStats(t),d=decisionOf(t),confidence=Math.round(Number(t.confidence||0)*100);detail.innerHTML=`<div class="eyebrow">r/${esc(t.community)} · 话题预览</div><h2>${esc(t.label_zh||t.label_en||'未命名话题')}</h2><div class="en">${esc(t.label_en||'')}</div><div class="tags"><span class="tag ${t.status==='formal'?'formal':'weak'}">${t.status==='formal'?'正式话题':'弱信号'}</span><span class="tag">趋势 ${esc(t.trend||'unknown')}</span><span class="tag">置信度 ${confidence}%</span></div><div class="summary">${esc(t.summary||'当前证据未形成摘要')}</div><div class="preview-stats"><div class="stat"><div class="k">支撑帖子</div><div class="v">${s.posts}</div></div><div class="stat"><div class="k">发帖作者</div><div class="v">${s.authors}</div></div><div class="stat"><div class="k">独立评论者</div><div class="v">${s.commenters}</div></div><div class="stat"><div class="k">采集评论</div><div class="v">${s.comments}</div></div></div><section class="field"><h3>一句话用户任务</h3>${list(arr(t.needs).slice(0,1),'当前证据未明确用户任务')}</section><section class="field"><h3>Top 3 痛点</h3>${list(arr(t.pains).slice(0,3))}</section><section class="field"><h3>当前主要解决方案</h3>${list(arr(t.current_solutions).slice(0,3))}</section><section class="field"><h3>Top 2 方案不足</h3>${list(arr(t.gaps).slice(0,2))}</section><section class="field"><h3>推荐产品决策类型</h3><div class="summary">${esc(d.type)}</div><div class="note ${d.status==='inference'?'status-inference':'status-unknown'}">${esc(d.rationale)}</div></section><div class="preview-action"><button id="open-report" class="primary" type="button">查看完整话题报告 →</button></div>`;document.getElementById('open-report').addEventListener('click',()=>openTopicReport(t.topic_id))}
function cards(items){return `<div class="report-grid">${items.map(([k,v])=>`<div class="report-card"><div class="k">${esc(k)}</div><div class="v">${esc(v??'未知')}</div></div>`).join('')}</div>`}
function showTopicDetail(t){const ev=arr(t.evidence),v=obj(t.demand_validation),s=topicStats(t),d=decisionOf(t),scope=obj(DATA.research_scope),coverage=obj(scope.coverage),communityCoverage=coverage[t.community]||coverage['r/'+t.community]||{},opportunities=arr(t.opportunity_hypotheses),support=arr(t.supporting_views),oppose=arr(t.opposing_views),risks=arr(t.risks);const evidenceHtml=ev.length?ev.map(e=>`<div class="evidence"><a href="${esc(e.url||'#')}" target="_blank" rel="noreferrer">打开 Reddit 原文 ↗</a><div class="en-text">${esc(e.claim_en||'')}</div><div class="zh">${esc(e.claim_zh||'未提供中文翻译')}</div><div class="row-meta">${esc(e.post_id||'')} · ${esc(e.evidence_id||'')}</div></div>`).join(''):'<div class="empty">暂无可回溯证据</div>';detail.innerHTML=`<div class="report-topbar"><button id="report-back" class="report-back" type="button">← 返回右侧预览</button><div class="report-kicker">COMMUNITY → TOPIC → EVIDENCE</div></div><div class="report-hero"><div><div class="eyebrow">r/${esc(t.community)} · 完整话题报告</div><h2>${esc(t.label_zh||t.label_en||'未命名话题')}</h2><div class="en">${esc(t.label_en||'')}</div><div class="tags"><span class="tag ${t.status==='formal'?'formal':'weak'}">${t.status==='formal'?'正式话题':'弱信号'}</span><span class="tag">趋势 ${esc(t.trend||'unknown')}</span><span class="tag">${s.posts} 帖 · ${s.participants} 位参与者 · ${s.comments} 评论</span></div><div class="summary">${esc(t.summary||'当前证据未形成结论摘要')}</div></div><div class="report-score"><div class="n">${esc(t.signal_score??'—')}<small>/10</small></div><b>社区信号分</b><div class="note">用于粗扫排序，不代表市场规模或开品结论。</div></div></div><section class="report-section"><h3>1. 结论摘要和研究范围</h3>${cards([['研究社区','r/'+t.community],['开始日期',scope.start_date||'未记录'],['结束日期',scope.end_date||'未记录'],['采集档位',scope.depth||'未记录']])}<div class="note">产品方向均为机会假设；没有业务数据时，价格、利润、制造工艺与供应链结论不作推断。</div></section><section class="report-section"><h3>2. 需求验证与时间趋势</h3>${cards([['支撑话题帖子',s.posts],['独立发帖作者',s.authors],['独立评论者',s.commenters],['独立讨论参与者',s.participants],['采集评论',s.comments],['被引用证据',s.evidence],['最近30天帖子',v.current_posts??t.current_post_count??0],['历史基线帖子',v.baseline_posts??t.baseline_post_count??0]])}<div class="note">${esc(v.note||'这些是本次采集到的社区样本信号，不是 Reddit 全量市场占有率。')}</div></section><section class="report-section"><h3>3. 谁在讨论：用户与使用场景</h3>${cards([['动力平台',arr(t.platforms).join('、')||'未知'],['车辆/年份',arr(t.vehicles).join('、')||'未知'],['现实条件与目的',arr(t.scenarios).join('、')||'未知'],['用户类型',arr(t.user_types).join('、')||'当前证据未单独确认']])}</section>${claimSection('4. 用户任务 · 想完成什么','needs',t,t.needs,'任务必须同时结合使用条件和目的，单独的车型或零件名不视为任务。')}${claimSection('5. 主要痛点','pains',t,t.pains,'当前结构仅呈现有证据的痛点；严重度未被原文支持时不补写。')}${claimSection('5A. 痛点后果','consequences',t,t.consequences,'后果必须由帖子或评论直接支持。')}${claimSection('6. 当前产品与解决办法','current_solutions',t,t.current_solutions)}${claimSection('7. 现有方案不足','gaps',t,t.gaps)}<section class="report-section"><h3>8. 产品决策</h3>${cards([['决策类型',d.type],['判断状态',d.status==='inference'?'AI推断，待验证':'未知/暂不形成机会'],['判断依据',d.rationale],['价格/利润/制造','待业务补充']])}</section><section class="report-section"><h3>9. 机会方向、支持证据、反对观点和风险</h3><div class="report-two"><div class="report-card"><div class="k">机会假设</div>${list(opportunities,'当前证据不支持具体产品机会')}</div><div class="report-card"><div class="k">支持观点</div>${list(support)}</div><div class="report-card"><div class="k">反对观点</div>${list(oppose)}</div><div class="report-card"><div class="k">风险</div>${list(risks,'当前证据未明确风险，需业务和工程验证')}</div></div></section><section class="report-section"><h3>10. SuncentAuto相关性</h3><div class="claim-row"><div class="claim-text status-unknown">${esc(obj(t.suncent_relevance).value||'待业务补充：需与公司现有品类、适配能力、供应链和售后数据交叉验证。')}</div></div></section><section class="report-section"><h3>11. 下一步验证</h3>${list(t.validation_questions,'先补充更多跨帖子证据，再决定是否进入产品验证')}</section><section class="report-section"><h3>12. 帖子 / 评论证据 <small>${ev.length} 条可回溯来源</small></h3><div class="evidence-list">${evidenceHtml}</div></section><section class="report-section"><h3>13. 数据覆盖与研究限制</h3>${cards([['请求范围',`${scope.start_date||'未知'} 至 ${scope.end_date||'未知'}`],['实际覆盖',`${communityCoverage.actual_start||'未知'} 至 ${communityCoverage.actual_end||'未知'}`],['覆盖状态',communityCoverage.status||'未记录'],['扫描帖子',obj(obj(DATA.report_metrics).communities)[t.community]?.scanned_post_count??'未记录']])}<div class="note">达到列表上限但未覆盖开始日期时为 partial；本报告只代表采集到的社区样本。删除帖、不可访问内容和采集失败可能造成遗漏。</div></section><div class="footer">本页与社区图谱、Excel 均读取同一份 analysis.json。事实、AI推断与未知必须分开理解；任何产品判断都需结合访谈、销量、退货、成本和工程验证。</div>`;document.getElementById('report-back').addEventListener('click',()=>returnToPreview())}
function updateMetrics(){const m=DATA.report_metrics||{};metrics.textContent=`${Number(m.community_count||0)} 社区 · ${Number(m.scanned_post_count||0)} 扫描帖 · ${Number(m.deep_read_post_count||0)} 深读帖 · ${Number(m.analyzed_post_count||0)} 分析帖 · ${Number(m.topic_count||0)} 话题 · ${Number(m.participant_count||0)} 参与者 · ${Number(m.evidence_count||0)} 证据`;const s=obj(DATA.research_scope);scopeLabel.textContent=s.start_date&&s.end_date?`${s.start_date} → ${s.end_date} · ${s.depth||''}`:'Community radar'}
search.addEventListener('input',()=>{renderSide();renderGraph()});reset.addEventListener('click',()=>closePanel());closeBtn.addEventListener('click',()=>currentView==='report'?returnToPreview():closePanel());svg.addEventListener('click',e=>{if(e.target===svg)closePanel()});window.addEventListener('hashchange',applyHash);updateMetrics();applyHash();
</script></body></html>'''
    output.write_text(html.replace("__PAYLOAD__", payload), encoding="utf-8")
    (output.parent / "community_topic_map.json").write_text(
        json.dumps(build_topic_map(document), ensure_ascii=False, indent=2), encoding="utf-8"
    )
    return output


def build_topic_map(analysis: Mapping[str, Any]) -> dict[str, Any]:
    """Return graph-ready community/topic nodes and edges."""
    topics = [item for item in analysis.get("topics", []) if isinstance(item, Mapping)]
    names: dict[str, str] = {}
    raw_names = [*analysis.get("communities", []), *(item.get("community", "") for item in topics)]
    for value in raw_names:
        display = str(value or "").removeprefix("r/")
        if display:
            names.setdefault(display.casefold(), display)
    communities = list(names.values())
    return {
        "nodes": (
            [{"id": f"community:{name}", "type": "community", "label": name} for name in communities]
            + [
                {"id": str(item.get("topic_id", "")), "type": "topic", "label": item.get("label_zh", item.get("label_en", "")), "community": item.get("community")}
                for item in topics
            ]
        ),
        "edges": [
            {"source": f"community:{str(item.get('community', '')).removeprefix('r/')}", "target": item.get("topic_id")}
            for item in topics if item.get("topic_id")
        ],
    }
