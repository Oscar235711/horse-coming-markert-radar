"""Offline Chinese community -> topic -> VOC evidence report."""

from __future__ import annotations

import json
from pathlib import Path
from typing import Any, Mapping


def render_html(analysis: Mapping[str, Any], output_path: str | Path) -> Path:
    """Render one canonical payload with a graph, preview drawer and full report."""
    output = Path(output_path)
    output.parent.mkdir(parents=True, exist_ok=True)
    payload = json.dumps(dict(analysis), ensure_ascii=False, separators=(",", ":"))
    payload = payload.replace("&", "\\u0026").replace("<", "\\u003c").replace(">", "\\u003e")
    html = r'''<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Opportunity Radar｜柴油皮卡改装 VOC</title>
<style>
:root{--bg:#f3f0ea;--paper:#fffdf9;--ink:#25221f;--muted:#817a71;--line:#ded7cd;--orange:#d66c32;--orange-dark:#a8461f;--green:#39735d;--blue:#4b6574;--shadow:0 20px 70px rgba(46,36,26,.16)}
*{box-sizing:border-box}html,body{height:100%;margin:0}body{overflow:hidden;background:var(--bg);color:var(--ink);font:14px/1.6 Inter,"Microsoft YaHei",system-ui,sans-serif}button,input{font:inherit}button{color:inherit}
header{height:60px;padding:0 25px;border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;background:rgba(247,244,239,.96);position:relative;z-index:5}.logo{font:800 18px/1 ui-monospace,monospace}.logo b{color:var(--orange)}.brand{font:11px ui-monospace,monospace;color:var(--muted);letter-spacing:.08em}
.page{height:calc(100% - 60px);display:grid;grid-template-columns:300px minmax(0,1fr) 0;transition:grid-template-columns .24s ease}.page.detail-open{grid-template-columns:300px minmax(360px,1fr) 440px}.page.report-open{grid-template-columns:280px minmax(320px,.6fr) minmax(650px,1.4fr)}
.left{padding:25px 20px;border-right:1px solid var(--line);overflow:auto;background:#f8f5f0}.left-kicker,.eyebrow{font:700 10px ui-monospace,monospace;letter-spacing:.12em;text-transform:uppercase;color:var(--orange-dark)}.left h2{font-size:24px;line-height:1.2;margin:8px 0}.intro{font-size:12px;color:var(--muted);margin:0 0 18px}.search{width:100%;padding:10px 12px;border:1px solid var(--line);border-radius:10px;background:#fff;outline:none}.search:focus{border-color:var(--orange)}.reset{border:0;background:none;color:var(--orange-dark);padding:12px 0;cursor:pointer}.section-title{font-size:10px;font-weight:800;color:var(--muted);letter-spacing:.08em;text-transform:uppercase;margin:16px 0 8px}.community-row,.topic-row{width:100%;border:0;background:transparent;border-radius:9px;padding:9px 8px;display:flex;gap:9px;text-align:left;cursor:pointer}.community-row:hover,.topic-row:hover,.topic-row.active{background:#eee8de}.dot,.topic-dot{width:11px;height:11px;border:2px solid #756d64;border-radius:50%;margin-top:4px;flex:none}.topic-dot{border:0;background:var(--orange)}.row-body{min-width:0;display:flex;flex-direction:column}.row-name{font-weight:700}.row-meta{font-size:11px;color:var(--muted)}.empty{color:var(--muted);font-size:12px;padding:12px 0}.legend{font-size:11px;color:var(--muted);border-top:1px solid var(--line);margin-top:20px;padding-top:14px}.legend-line{display:flex;gap:8px;margin:6px 0}
.center{position:relative;min-width:0;overflow:hidden;background:radial-gradient(circle at 50% 45%,#fbfaf7 0,#f1ede7 66%)}.toolbar{position:absolute;left:18px;top:16px;z-index:2}.metric{display:block;max-width:900px;border:1px solid var(--line);background:rgba(255,255,255,.84);padding:7px 10px;border-radius:8px;font:11px ui-monospace,monospace;color:#645d55}.graph{width:100%;height:100%}.graph-help{position:absolute;bottom:20px;left:50%;transform:translateX(-50%);font-size:11px;color:var(--muted);background:rgba(255,255,255,.88);padding:7px 12px;border-radius:999px}.edge{stroke:#d7c9b9;stroke-width:1}.community-node,.topic-node{cursor:pointer}.community-node circle{fill:#f8f6f1;stroke:#796f65;stroke-width:1.5}.community-node.active circle{fill:#fff5e9;stroke:var(--orange);stroke-width:2}.node-label,.node-count,.topic-label{text-anchor:middle;pointer-events:none}.node-label{font-size:13px;font-weight:800}.node-count{font-size:10px;fill:#8c847a}.topic-label{font-size:11px;fill:#554e47}.topic-node circle{stroke:#fff;stroke-width:2}.topic-node.active circle{stroke:#7a3c12;stroke-width:3}
.right{position:relative;min-width:0;border-left:1px solid var(--line);background:var(--paper);overflow:auto;box-shadow:var(--shadow);transition:opacity .2s}.right.closed{opacity:0;pointer-events:none}.close{position:sticky;float:right;right:16px;top:14px;z-index:8;width:32px;height:32px;border:1px solid var(--line);border-radius:50%;background:#fff;cursor:pointer}.detail{padding:30px 28px 60px}.detail h2{font-size:28px;line-height:1.18;margin:8px 40px 5px 0}.en{font-size:12px;color:var(--muted)}.summary{font-size:15px;line-height:1.75;margin:16px 0}.tags{display:flex;gap:7px;flex-wrap:wrap;margin:13px 0}.tag{font-size:10px;background:#eee9e1;border-radius:999px;padding:4px 8px}.tag.formal{background:#e2efe8;color:#2b6b4f}.tag.weak{background:#f6eadb;color:#87521f}.field{border-top:1px solid var(--line);padding:15px 0}.field h3,.report-section h3{font-size:14px;margin:0 0 9px}.field ul,.report-section ul{margin:0;padding-left:18px}.field li,.report-section li{margin:5px 0}.preview-stats{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin:15px 0}.stat{border:1px solid var(--line);background:#fff;border-radius:9px;padding:10px}.stat .k{font-size:10px;color:var(--muted)}.stat .v{font-size:18px;font-weight:800}.preview-action{position:sticky;bottom:-60px;margin:22px -28px -60px;padding:30px 28px 14px;background:linear-gradient(transparent,var(--paper) 24%)}.primary{width:100%;border:0;border-radius:9px;background:var(--ink);color:#fff;padding:12px;cursor:pointer;font-weight:700}.primary:hover{background:var(--orange-dark)}.topic-mini{display:flex;gap:9px;padding:9px 0;border-bottom:1px solid #ebe6df;cursor:pointer}.topic-mini span:nth-child(2){flex:1}
.page.report-open .detail{padding:28px 38px 70px}.report-topbar{display:flex;justify-content:space-between;align-items:center;padding-bottom:15px;border-bottom:1px solid var(--line)}.report-back{border:0;background:none;color:var(--orange-dark);cursor:pointer;padding:0}.report-kicker{font:10px ui-monospace,monospace;color:var(--muted)}.report-hero{padding:28px 0 20px;display:grid;grid-template-columns:1fr 190px;gap:25px}.report-hero h2{font-size:35px;margin:6px 0}.report-score{border-left:3px solid var(--orange);background:#fff5e9;padding:15px;border-radius:0 9px 9px 0}.report-score .n{font-size:31px;font-weight:800}.report-score small{font-size:12px}.report-section{padding:23px 0;border-top:1px solid var(--line)}.report-section h3{font-size:17px}.report-section h3 small{font-size:11px;color:var(--muted);font-weight:500}.report-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:9px}.report-card{border:1px solid var(--line);background:#fff;border-radius:9px;padding:12px}.report-card .k{font-size:10px;color:var(--muted)}.report-card .v{font-weight:750;margin-top:4px}.report-two{display:grid;grid-template-columns:1fr 1fr;gap:10px}.claim-row{border:1px solid var(--line);background:#fff;border-radius:9px;padding:12px;margin:8px 0}.claim-text{font-weight:650}.claim-explanation{color:#5d564f;margin-top:5px}.claim-meta{font-size:11px;color:var(--muted);margin-top:7px}.claim-source{font-size:11px;margin-top:7px}.claim-source a,.evidence a{color:var(--orange-dark)}.note{font-size:11px;color:#745b3f;background:#f8eedf;border-radius:7px;padding:9px 11px;margin-top:9px}.evidence-list{display:grid;grid-template-columns:1fr 1fr;gap:10px}.evidence{border:1px solid var(--line);background:#fff;border-radius:9px;padding:12px}.evidence .en-text{font-size:13px;margin-top:8px}.evidence .zh{font-size:12px;color:#4b6574;margin-top:5px}.evidence details{margin-top:8px}.status-fact{color:var(--green)}.status-inference{color:#936223}.status-unknown{color:var(--muted)}.footer{font-size:11px;color:var(--muted);border-top:1px solid var(--line);padding-top:16px;margin-top:25px}
@media(max-width:1000px){.page.detail-open,.page.report-open{grid-template-columns:250px 0 minmax(0,1fr)}.report-grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:700px){header .brand{display:none}.page,.page.detail-open,.page.report-open{grid-template-columns:0 minmax(0,1fr) 0}.page.detail-open,.page.report-open{grid-template-columns:0 0 minmax(0,1fr)}.left{display:none}.detail,.page.report-open .detail{padding:25px 20px 60px}.report-hero,.report-two,.evidence-list{grid-template-columns:1fr}}
</style></head><body>
<header><div class="logo">Opportunity Radar<b>.</b></div><div class="brand">SUNCENTAUTO · DIESEL PICKUP VOC</div></header>
<div class="page" id="page">
  <aside class="left"><div class="left-kicker" id="scopeLabel">Community radar</div><h2>社区—话题图谱</h2><p class="intro">先选择社区，再查看该社区内有足够证据的话题。点击话题只打开右侧预览，完整报告由按钮进入。</p><div id="questionBanner" class="note" style="display:none"></div><input id="search" class="search" placeholder="搜索社区、话题、车型或关键词…" autocomplete="off"><button id="reset" class="reset" type="button">← 回到全局社区图</button><div class="section-title">本次出现的社区</div><div id="communityList"></div><div class="section-title" id="topicTitle">选择社区后显示话题</div><div id="topicList"><div class="empty">当前未选择社区</div></div><div class="legend"><div class="legend-line"><i class="dot"></i><span>空心圆：社区</span></div><div class="legend-line"><i class="topic-dot"></i><span>实心圆：正式话题</span></div><div class="legend-line">弱信号保留在数据中，不自动包装成产品结论</div></div></aside>
  <main class="center"><div class="toolbar"><span class="metric" id="metrics"></span></div><svg id="graph" class="graph" viewBox="0 0 1000 720" role="img" aria-label="社区与话题关系图"></svg><div class="graph-help" id="graphHelp">点击一个社区，展开它的话题</div></main>
  <aside id="right" class="right closed"><button class="close" id="close" type="button" aria-label="关闭">×</button><div id="detail" class="detail"></div></aside>
</div>
<script id="analysis-data" type="application/json">__PAYLOAD__</script>
<script>
const DATA=JSON.parse(document.getElementById('analysis-data').textContent||'{}');
const ALL_TOPICS=Array.isArray(DATA.topics)?DATA.topics.filter(Boolean):[]; const formalTopics=ALL_TOPICS.filter(t=>t.status==='formal'); const TOPICS=formalTopics.length?formalTopics:ALL_TOPICS;
const norm=v=>String(v??'').trim().toLowerCase().replace(/^r\//,''); const arr=v=>Array.isArray(v)?v.filter(Boolean):[]; const obj=v=>v&&typeof v==='object'&&!Array.isArray(v)?v:{}; const num=v=>Number.isFinite(Number(v))?Number(v):0;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const communityNames=new Map(); for(const raw of arr(DATA.communities).concat(TOPICS.map(t=>t.community||'')).filter(Boolean).map(String)){const key=norm(raw);if(!communityNames.has(key))communityNames.set(key,raw.replace(/^r\//i,''))} const COMMUNITIES=Array.from(communityNames.values()); const byCommunity=new Map(COMMUNITIES.map(c=>[norm(c),TOPICS.filter(t=>norm(t.community)===norm(c))]));
const page=document.getElementById('page'),svg=document.getElementById('graph'),search=document.getElementById('search'),reset=document.getElementById('reset'),closeBtn=document.getElementById('close'),right=document.getElementById('right'),detail=document.getElementById('detail'),communityList=document.getElementById('communityList'),topicList=document.getElementById('topicList'),topicTitle=document.getElementById('topicTitle'),metrics=document.getElementById('metrics'),graphHelp=document.getElementById('graphHelp'),scopeLabel=document.getElementById('scopeLabel'),questionBanner=document.getElementById('questionBanner'); const NS='http://www.w3.org/2000/svg'; let selectedCommunity='',selectedTopic='',currentView='graph',applyingHash=false;
const communityTopics=c=>byCommunity.get(norm(c))||[]; const findTopic=id=>TOPICS.find(t=>String(t.topic_id)===String(id)); const claims=(t,key,legacy=[])=>{const direct=arr(t[key]);if(direct.length)return direct;const nested=arr(obj(t.claim_evidence)[key]);if(nested.length)return nested;return arr(legacy).map(text=>({text}))}; const claimText=c=>typeof c==='object'?String(c.text||c.value||''):String(c||''); const evidenceOf=c=>arr(obj(c).evidence);
function setHash(view){if(applyingHash)return;const p=new URLSearchParams();if(selectedCommunity)p.set('community',selectedCommunity);if(selectedTopic)p.set('topic',selectedTopic);if(view&&view!=='graph')p.set('view',view);const next=p.toString();if(location.hash.slice(1)!==next)location.hash=next} function openPanel(){right.classList.remove('closed');page.classList.add('detail-open')} function closePanel(sync=true){selectedCommunity='';selectedTopic='';currentView='graph';right.classList.add('closed');page.classList.remove('detail-open','report-open');renderSide();renderGraph();if(sync)setHash('graph')}
function selectCommunity(name,sync=true){selectedCommunity=String(name).replace(/^r\//i,'');selectedTopic='';currentView='community';page.classList.remove('report-open');openPanel();renderSide();renderGraph();showCommunityDetail(selectedCommunity);if(sync)setHash('community')} function selectTopic(id,sync=true){const t=findTopic(id);if(!t)return;selectedCommunity=String(t.community).replace(/^r\//i,'');selectedTopic=String(t.topic_id);currentView='preview';page.classList.remove('report-open');openPanel();renderSide();renderGraph();showTopicPreview(t);if(sync)setHash('preview')} function openTopicReport(id=selectedTopic,sync=true){const t=findTopic(id);if(!t)return;selectedCommunity=String(t.community).replace(/^r\//i,'');selectedTopic=String(t.topic_id);currentView='report';openPanel();page.classList.add('report-open');renderSide();renderGraph();showTopicDetail(t);if(sync)setHash('report')} function returnToPreview(sync=true){const t=findTopic(selectedTopic);if(!t){closePanel(sync);return}currentView='preview';page.classList.remove('report-open');showTopicPreview(t);renderSide();renderGraph();if(sync)setHash('preview')}
function applyHash(){applyingHash=true;try{const p=new URLSearchParams(location.hash.slice(1)),community=p.get('community'),topic=p.get('topic'),view=p.get('view');if(topic&&findTopic(topic)){view==='report'?openTopicReport(topic,false):selectTopic(topic,false)}else if(community&&COMMUNITIES.some(c=>norm(c)===norm(community))){selectCommunity(community,false)}else closePanel(false)}finally{applyingHash=false}}
function renderSide(){const q=search.value.trim().toLowerCase();communityList.innerHTML=COMMUNITIES.filter(c=>!q||norm(c).includes(q)||communityTopics(c).some(t=>JSON.stringify(t).toLowerCase().includes(q))).map(c=>`<button class="community-row" data-community="${esc(c)}"><i class="dot"></i><span class="row-body"><span class="row-name">r/${esc(c)}</span><span class="row-meta">${communityTopics(c).length} 个话题</span></span></button>`).join('')||'<div class="empty">没有匹配社区</div>';communityList.querySelectorAll('[data-community]').forEach(b=>b.addEventListener('click',()=>selectCommunity(b.dataset.community)));if(!selectedCommunity){topicTitle.textContent='选择社区后显示话题';topicList.innerHTML='<div class="empty">当前未选择社区</div>';return}const ts=communityTopics(selectedCommunity).filter(t=>!q||JSON.stringify(t).toLowerCase().includes(q));topicTitle.textContent=`r/${selectedCommunity} · ${communityTopics(selectedCommunity).length} 个话题`;topicList.innerHTML=ts.length?ts.slice().sort((a,b)=>num(b.heat_score)-num(a.heat_score)).map(t=>`<button class="topic-row ${String(t.topic_id)===selectedTopic?'active':''}" data-topic="${esc(t.topic_id)}"><i class="topic-dot"></i><span class="row-body"><span class="row-name">${esc(t.label_zh||t.label_en||'未命名话题')}</span><span class="row-meta">${esc(t.trend_card?.trend_label||t.trend||'未知')} · 热度 ${esc(t.heat_score||0)} · ${esc(t.post_count||0)} 帖</span></span></button>`).join(''):'<div class="empty">该社区暂无达到证据门槛的话题</div>';topicList.querySelectorAll('[data-topic]').forEach(b=>b.addEventListener('click',()=>selectTopic(b.dataset.topic)))}
function communityPositions(){const n=COMMUNITIES.length||1,out=[];for(let i=0;i<n;i++){const angle=-Math.PI/2+Math.PI*2*i/n,r=n<=2?170:240;out.push({x:500+Math.cos(angle)*r,y:355+Math.sin(angle)*r})}return out} function topicPositions(ts){const out=[];let index=0,ring=0;while(index<ts.length){const count=Math.min(7,ts.length-index),radius=145+ring*90;for(let j=0;j<count;j++){const angle=-Math.PI/2+Math.PI*2*j/count+(ring%2?Math.PI/7:0);out.push({x:500+Math.cos(angle)*radius,y:360+Math.sin(angle)*radius});index++}ring++}return out}
function renderGraph(){svg.innerHTML='';const edges=document.createElementNS(NS,'g'),nodes=document.createElementNS(NS,'g');svg.append(edges,nodes);if(!selectedCommunity){graphHelp.textContent='点击一个社区，展开它的话题';communityPositions().forEach((p,i)=>{const c=COMMUNITIES[i],ts=communityTopics(c),r=36+Math.min(32,ts.length*4),g=document.createElementNS(NS,'g');g.setAttribute('class','community-node');const circle=document.createElementNS(NS,'circle');circle.setAttribute('cx',p.x);circle.setAttribute('cy',p.y);circle.setAttribute('r',r);g.append(circle);const label=document.createElementNS(NS,'text');label.setAttribute('x',p.x);label.setAttribute('y',p.y+4);label.setAttribute('class','node-label');label.textContent='r/'+c;g.append(label);const count=document.createElementNS(NS,'text');count.setAttribute('x',p.x);count.setAttribute('y',p.y+r+18);count.setAttribute('class','node-count');count.textContent=ts.length+' 个话题';g.append(count);g.addEventListener('click',()=>selectCommunity(c));nodes.append(g)});return}graphHelp.textContent='点击话题先看右侧预览，再进入完整报告';const ts=communityTopics(selectedCommunity),center={x:500,y:360},cr=42+Math.min(30,ts.length*3),cg=document.createElementNS(NS,'g');cg.setAttribute('class','community-node active');const cc=document.createElementNS(NS,'circle');cc.setAttribute('cx',center.x);cc.setAttribute('cy',center.y);cc.setAttribute('r',cr);cg.append(cc);const cl=document.createElementNS(NS,'text');cl.setAttribute('x',center.x);cl.setAttribute('y',center.y+4);cl.setAttribute('class','node-label');cl.textContent='r/'+selectedCommunity;cg.append(cl);const ccount=document.createElementNS(NS,'text');ccount.setAttribute('x',center.x);ccount.setAttribute('y',center.y+cr+18);ccount.setAttribute('class','node-count');ccount.textContent=ts.length+' 个话题';cg.append(ccount);nodes.append(cg);topicPositions(ts).forEach((p,i)=>{const t=ts[i],r=12+Math.min(18,Math.sqrt(num(t.heat_score))*1.1);const line=document.createElementNS(NS,'line');line.setAttribute('x1',center.x);line.setAttribute('y1',center.y);line.setAttribute('x2',p.x);line.setAttribute('y2',p.y);line.setAttribute('class','edge');edges.append(line);const g=document.createElementNS(NS,'g');g.setAttribute('class','topic-node '+(String(t.topic_id)===selectedTopic?'active':''));const circle=document.createElementNS(NS,'circle');circle.setAttribute('cx',p.x);circle.setAttribute('cy',p.y);circle.setAttribute('r',r);circle.setAttribute('fill',t.status==='formal'?'#d66c32':'#cda06b');g.append(circle);const label=document.createElementNS(NS,'text');label.setAttribute('x',p.x);label.setAttribute('y',p.y+r+16);label.setAttribute('class','topic-label');const raw=String(t.label_zh||t.label_en||'未命名话题');label.textContent=raw.length>18?raw.slice(0,17)+'…':raw;g.append(label);g.addEventListener('click',e=>{e.stopPropagation();selectTopic(t.topic_id)});nodes.append(g)})}
function list(values,empty='当前证据未确认'){const v=arr(values).map(claimText).filter(Boolean);return `<ul>${v.length?v.map(x=>`<li>${esc(x)}</li>`).join(''):`<li class="status-unknown">${esc(empty)}</li>`}</ul>`} function metricStats(t){const snap=obj(t.data_snapshot);return {posts:num(snap.supporting_posts||t.post_count),authors:num(snap.authors||t.author_count),commenters:num(snap.commenters||t.commenter_count),participants:num(snap.participants||t.participant_count),comments:num(snap.comments||t.collected_comment_count),evidence:num(snap.evidence||arr(t.evidence).length)}} function trend(t){const x=obj(t.trend_card),growth=x.growth_rate;return {label:x.trend_label||({'new':'新出现','rising':'上升','stable':'稳定','falling':'下降'}[t.trend]||'未知'),current:num(x.current_30d_posts??t.current_post_count),baseline:num(x.baseline_60d_posts??t.baseline_post_count),growth:growth===null||growth===undefined?'未知':`${Math.round(num(growth)*100)}%`}}
function claimView(c){const ce=obj(c),ev=evidenceOf(ce),meta=[ce.frequency?`证据 ${esc(ce.frequency)} 次`:ev.length?`证据 ${ev.length} 条`:'',ce.severity&&ce.severity!=='unknown'?`严重度 ${esc(ce.severity)}`:'',ce.post_count?`${esc(ce.post_count)} 帖`:''].filter(Boolean).join(' · ');return `<div class="claim-row"><div class="claim-text">${esc(claimText(c)||'当前证据不足')}</div>${ce.explanation?`<div class="claim-explanation">${esc(ce.explanation)}</div>`:''}${ce.consequence&&ce.consequence!=='unknown'?`<div class="claim-explanation">后果：${esc(ce.consequence)}</div>`:''}${meta?`<div class="claim-meta">${meta}</div>`:''}${ev.length?`<div class="claim-source">${ev.slice(0,3).map(e=>`<a href="${esc(obj(e).url||'#')}" target="_blank" rel="noreferrer">查看证据 ↗</a>`).join(' · ')}</div>`:''}</div>`}
function claimSection(title,key,t,legacy=[],note=''){const rows=claims(t,key,legacy);return `<section class="report-section"><h3>${esc(title)}</h3>${rows.length?rows.slice(0,8).map(claimView).join(''):list([], '当前证据不足，未形成该项结论')}${rows.length>8?`<div class="note">其余 ${rows.length-8} 条断言仍保留在 analysis.json 和 Excel。</div>`:''}${note?`<div class="note">${esc(note)}</div>`:''}</section>`}
function evidenceBrowser(ev){const unique=[],seen=new Set();for(const item of ev){const key=String(item.evidence_id||`${item.post_id}:${item.url}`);if(!seen.has(key)){seen.add(key);unique.push(item)}}if(!unique.length)return '<div class="empty">暂无可回溯证据</div>';return `<details class="evidence-details"><summary>展开 ${unique.length} 条证据（默认只看代表摘录）</summary><div class="evidence-list">${unique.slice(0,8).map(e=>`<div class="evidence"><a href="${esc(e.url||'#')}" target="_blank" rel="noreferrer">打开 Reddit 原文 ↗</a><div class="en-text">${esc(e.quote_original||e.claim_en||e.claim||'')}</div><div class="zh">${esc(e.translation_zh||e.claim_zh||'未生成中文翻译')}</div><div class="row-meta">${esc(e.post_id||'')} · ${esc(e.evidence_id||'')}</div></div>`).join('')}</div>${unique.length>8?`<div class="note">页面展示前 8 条代表证据，其余 ${unique.length-8} 条仍在 Excel 和 analysis.json 中。</div>`:''}</details>`}
function showTopicPreview(t){const s=metricStats(t),tr=trend(t),d=obj(t.product_decision),tasks=claims(t,'user_tasks',t.jtbd_cards||t.needs),pains=claims(t,'pain_point_cards',t.pains),solutions=claims(t,'solution_cards',t.current_solutions),gaps=claims(t,'gap_cards',t.gaps);detail.innerHTML=`<div class="eyebrow">r/${esc(t.community)} · 话题预览</div><h2>${esc(t.label_zh||t.label_en||'未命名话题')}</h2><div class="en">${esc(t.label_en||'')}</div><div class="tags"><span class="tag ${t.status==='formal'?'formal':'weak'}">${t.status==='formal'?'正式话题':'弱信号'}</span><span class="tag">趋势 ${esc(tr.label)}</span><span class="tag">置信度 ${Math.round(num(t.confidence)*100)}%</span></div><div class="summary">${esc(t.summary_zh||t.summary||'当前证据未形成结论摘要')}</div><section class="field"><h3>数据概览</h3><div class="preview-stats"><div class="stat"><div class="k">支撑帖子</div><div class="v">${s.posts}</div></div><div class="stat"><div class="k">发帖作者</div><div class="v">${s.authors}</div></div><div class="stat"><div class="k">评论者</div><div class="v">${s.commenters}</div></div><div class="stat"><div class="k">评论数量</div><div class="v">${s.comments}</div></div></div></section><section class="field"><h3>趋势卡</h3><div class="claim-row"><div class="claim-text">${esc(tr.label)}</div><div class="claim-meta">最近30天 ${tr.current} 帖 · 前60天 ${tr.baseline} 帖 · 变化 ${esc(tr.growth)}</div></div></section><section class="field"><h3>Seller Insight</h3><div class="summary">${esc(t.seller_insight_zh||'当前证据未形成卖家洞察')}</div></section><section class="field"><h3>用户任务</h3>${tasks.length?tasks.slice(0,1).map(claimView).join(''):list([], '当前证据未明确用户任务')}</section><section class="field"><h3>Top 3 Pain Points</h3>${pains.length?pains.slice(0,3).map(claimView).join(''):list([], '当前证据未形成明确痛点')}</section><section class="field"><h3>当前解决方案</h3>${solutions.length?solutions.slice(0,3).map(claimView).join(''):list([], '当前证据未确认解决方案')}</section><section class="field"><h3>方案不足</h3>${gaps.length?gaps.slice(0,2).map(claimView).join(''):list([], '当前证据未确认方案不足')}</section><section class="field"><h3>产品判断</h3><div class="claim-row"><div class="claim-text">${esc(d.type||'暂不形成产品机会')}</div><div class="claim-explanation">${esc(d.rationale||'当前证据不足，暂不形成产品判断。')}</div></div></section><div class="preview-action"><button id="open-report" class="primary" type="button">查看完整话题报告 →</button></div>`;document.getElementById('open-report').addEventListener('click',()=>openTopicReport(t.topic_id))}
function cards(items){return `<div class="report-grid">${items.map(([k,v])=>`<div class="report-card"><div class="k">${esc(k)}</div><div class="v">${esc(v??'未知')}</div></div>`).join('')}</div>`}
function showTopicDetail(t){const s=metricStats(t),tr=trend(t),scope=obj(DATA.research_scope),coverage=obj(scope.coverage),cc=coverage[t.community]||coverage['r/'+t.community]||{},d=obj(t.product_decision),ev=arr(t.evidence),tasks=claims(t,'user_tasks',t.jtbd_cards||t.needs),scenes=claims(t,'scene_cards',t.scenarios),needs=claims(t,'need_cards',t.needs),pains=claims(t,'pain_point_cards',t.pains),solutions=claims(t,'solution_cards',t.current_solutions),gaps=claims(t,'gap_cards',t.gaps),opps=claims(t,'opportunity_hypotheses',t.opportunity_hypotheses),support=arr(t.supporting_views),oppose=arr(t.opposing_views),questions=arr(t.validation_questions);detail.innerHTML=`<div class="report-topbar"><button id="report-back" class="report-back" type="button">← 返回右侧预览</button><div class="report-kicker">社区 → 话题 → 用户洞察 → 证据</div></div><div class="report-hero"><div><div class="eyebrow">r/${esc(t.community)} · 完整话题报告</div><h2>${esc(t.label_zh||t.label_en||'未命名话题')}</h2><div class="en">${esc(t.label_en||'')}</div><div class="tags"><span class="tag ${t.status==='formal'?'formal':'weak'}">${t.status==='formal'?'正式话题':'弱信号'}</span><span class="tag">趋势 ${esc(tr.label)}</span><span class="tag">${s.posts} 帖 · ${s.participants} 位参与者 · ${s.comments} 评论</span></div><div class="summary">${esc(t.summary_zh||t.summary||'当前证据未形成结论摘要')}</div></div><div class="report-score"><div class="n">${esc(t.signal_score??'—')}<small>/10</small></div><b>社区信号分</b><div class="note">用于社区样本排序，不代表市场规模。</div></div></div><section class="report-section"><h3>1. 结论摘要与数据范围</h3>${cards([['研究社区','r/'+t.community],['开始日期',scope.start_date||'未记录'],['结束日期',scope.end_date||'未记录'],['支撑帖子',s.posts],['发帖作者',s.authors],['评论者',s.commenters],['评论数量',s.comments],['被引用证据',s.evidence]])}<div class="claim-row"><div class="claim-text">Seller Insight</div><div class="claim-explanation">${esc(t.seller_insight_zh||'当前证据未形成卖家洞察')}</div></div></section><section class="report-section"><h3>2. 需求验证与趋势卡</h3>${cards([['趋势状态',tr.label],['最近30天帖子',tr.current],['前60天基线',tr.baseline],['变化幅度',tr.growth],['证据强度',`${s.evidence} 条`]])}<div class="note">趋势只代表本次 Reddit 社区样本信号，不等同于全市场占有率。</div></section><section class="report-section"><h3>3. 用户与使用场景：谁在讨论</h3>${cards([['动力平台',arr(t.platforms).join('、')||'未知'],['车辆/年份',arr(t.vehicles).join('、')||'未知'],['用户类型',arr(t.user_types).join('、')||'当前证据未单独确认']])}${scenes.length?scenes.slice(0,6).map(claimView).join(''):`<div class="claim-row status-unknown">当前证据未形成具体场景</div>`}</section>${claimSection('4. 用户任务（JTBD）','user_tasks',t,tasks)}${claimSection('5. 用户需求','need_cards',t,needs)}${claimSection('6. 主要痛点（Pain Points）','pain_point_cards',t,pains,'每条痛点都显示解释、频次、严重度、后果与证据。')}${claimSection('7. 痛点后果','consequences',t,t.consequences)}${claimSection('8. 当前产品与解决办法','solution_cards',t,solutions)}${claimSection('9. 现有方案不足：为何不够','gap_cards',t,gaps)}<section class="report-section"><h3>10. 产品决策与机会假设</h3>${cards([['决策类型',d.type||'暂不形成产品机会'],['判断状态',d.status==='inference'?'AI证据推断，待验证':'未知/暂不形成机会'],['判断依据',d.rationale||'当前证据不足'],['价格、利润、制造','待业务补充']])}${opps.length?opps.slice(0,5).map(claimView).join(''):`<div class="claim-row status-unknown">当前证据不支持具体产品机会</div>`}</section><section class="report-section"><h3>11. SuncentAuto相关性与下一步验证问题</h3><div class="claim-row"><div class="claim-text">${esc(obj(t.suncent_relevance).value||'待业务补充')}</div></div><div class="report-two"><div class="report-card"><div class="k">支持观点</div>${list(support)}</div><div class="report-card"><div class="k">反对观点</div>${list(oppose)}</div></div>${list(questions,'当前没有额外验证问题')}</section><section class="report-section"><h3>12. 帖子 / 评论证据 <small>${ev.length} 条可回溯来源</small></h3>${evidenceBrowser(ev)}</section><section class="report-section"><h3>13. 数据覆盖与研究限制</h3>${cards([['请求范围',`${scope.start_date||'未知'} 至 ${scope.end_date||'未知'}`],['实际覆盖',`${cc.actual_start||'未知'} 至 ${cc.actual_end||'未知'}`],['覆盖状态',cc.status||'未记录'],['扫描帖子',obj(obj(DATA.report_metrics).communities)[t.community]?.scanned_post_count??'未记录']])}<div class="note">删除帖、不可访问内容和采集失败可能造成遗漏；本报告只代表采集到的社区样本。</div></section><div class="footer">本页、社区图谱和 Excel 均读取同一份 analysis.json。事实、AI推断和未知分开呈现；产品判断均为机会假设。</div>`;document.getElementById('report-back').addEventListener('click',()=>returnToPreview())}
function updateMetrics(){const m=DATA.report_metrics||{};metrics.textContent=`${num(m.community_count)} 社区 · ${num(m.scanned_post_count)} 扫描帖 · ${num(m.deep_read_post_count)} 深读帖 · ${num(m.analyzed_post_count)} 分析帖 · ${num(m.topic_count)} 话题 · ${num(m.participant_count)} 参与者 · ${num(m.evidence_count)} 证据`;const s=obj(DATA.research_scope);scopeLabel.textContent=s.start_date&&s.end_date?`${s.start_date} → ${s.end_date} · ${s.depth||''}`:'Community radar';const q=String(DATA.research_question||'').trim();if(q){questionBanner.textContent='本次研究问题：'+q;questionBanner.style.display='block'}} search.addEventListener('input',()=>{renderSide();renderGraph()});reset.addEventListener('click',()=>closePanel());closeBtn.addEventListener('click',()=>currentView==='report'?returnToPreview():closePanel());svg.addEventListener('click',e=>{if(e.target===svg)closePanel()});window.addEventListener('hashchange',applyHash);updateMetrics();renderSide();renderGraph();applyHash();
</script></body></html>'''
    output.write_text(html.replace("__PAYLOAD__", payload), encoding="utf-8")
    (output.parent / "community_topic_map.json").write_text(json.dumps(build_topic_map(dict(analysis)), ensure_ascii=False, indent=2), encoding="utf-8")
    return output


def build_topic_map(analysis: Mapping[str, Any]) -> dict[str, Any]:
    """Return one case-insensitive graph projection for the report."""
    topics = [item for item in analysis.get("topics", []) if isinstance(item, Mapping)]
    names: dict[str, str] = {}
    for value in [*analysis.get("communities", []), *(item.get("community", "") for item in topics)]:
        display = str(value or "").removeprefix("r/")
        if display:
            names.setdefault(display.casefold(), display)
    communities = list(names.values())
    return {
        "nodes": ([{"id": f"community:{name}", "type": "community", "label": name} for name in communities]
                   + [{"id": str(item.get("topic_id", "")), "type": "topic", "label": item.get("label_zh", item.get("label_en", "")), "community": item.get("community")} for item in topics if item.get("topic_id")]),
        "edges": [{"source": f"community:{str(item.get('community', '')).removeprefix('r/')}", "target": item.get("topic_id")} for item in topics if item.get("topic_id")],
    }
